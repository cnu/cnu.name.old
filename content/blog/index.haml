---
title: Blog
pagetype: blog
---

- sorted_articles.each do |post|
  .post
    %h2= link_to post[:title], post.path
    %aside= "Posted at:  #{get_pretty_date(post)}"
    %article
      = get_excerpt(post)
